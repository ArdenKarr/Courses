#include <iostream>

using namespace std;

int **snaping(int n)
{
    int **matrix = new int*[n];
    for (int i = 0; i < n; i++)
        matrix[i] = new int[n];
    if (n / 2 == 0)
    {
        for (int k = n / 2; k > 0; k--)
        {
            
        }
    }
    else
    {
        
    }
}

int main()
{
    
    return 0;
}
